import './App.css'
import {Button} from "@/components/ui/button.tsx";

function App() {

  return (
  <Button variant={'secondary'} size={'lg'} className={'text-blue-500'}>Click Me</Button>
  )
}

export default App
